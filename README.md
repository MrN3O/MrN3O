<!-- If you add a GIF named `intro.gif` to the repository root, it will display here on GitHub -->
<p align="center">
  <!-- wrapper keeps border tightly matched to the image/video size -->
  <span style="display:inline-block;border:6px solid #0f0;padding:0;margin:0;line-height:0;border-radius:4px;box-sizing:content-box;background:#000;">
    <img src="intro.gif" alt="Matrix intro" style="display:block;max-width:100%;height:auto;vertical-align:top;" />
  </span>
</p>

<h1 align="center">Welcome to the Matrix of <code>MrN3O</code></h1>

<p align="center">Take the 💊 red pill, and see how deep the rabbit hole goes..</p>

---

Notes
- GitHub READMEs do not run JavaScript, so this file shows a static, centered intro that matches the site copy.
- For the full animated Matrix-style intro, open <code>index.html</code> in your browser or publish the repo with GitHub Pages and host the HTML page there.

Quick preview
- Open the repository folder and double-click <code>index.html</code>.
- Or deploy to GitHub Pages (Settings → Pages) and link the hosted page in your profile.

If you want a GIF or an animated image to embed directly in the README (so the animation plays inside GitHub), I can create one and add it to the repo — tell me if you'd like that.

  .matrix {
<!-- If you add a GIF named `intro.gif` to the repository root, it will display here on GitHub -->
<p align="center">
  <!-- wrapper keeps border tightly matched to the image/video size -->
  <span style="display:inline-block;border:6px solid #0f0;padding:6px;border-radius:4px;box-sizing:content-box;">
    <img src="intro.gif" alt="Matrix intro" style="display:block;max-width:100%;height:auto;" />
  </span>
</p>

<h1 align="center">Welcome to the Matrix of <code>MrN3O</code></h1>

<p align="center">Take the  red pill, and see how deep the rabbit hole goes..</p>

---

Notes
- GitHub READMEs do not run JavaScript, so this file shows a static, centered intro that matches the site copy.
- For the full animated Matrix-style intro, open <code>index.html</code> in your browser or publish the repo with GitHub Pages and host the HTML page there.

Quick preview
- Open the repository folder and double-click <code>index.html</code>.
- Or deploy to GitHub Pages (Settings  Pages) and link the hosted page in your profile.

If you want a GIF or an animated image to embed directly in the README (so the animation plays inside GitHub), I can create one and add it to the repo — tell me if you'd like that.

  .matrix {

